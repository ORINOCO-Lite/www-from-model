---
title: "{{ title }}"
persons:
{% for ass in associated_with if ass.object.schema_type == "xyzri:XYZPerson" -%}
- {{ ass.object.pid|replace('xyzrins:persons/', '') }}
{% endfor -%}
---

{{ description }}

{% if part_of %}This project is part of{% for p in part_of %} [{{ p.title }}](/{{ p.pid|replace('xyzrins:', '') }}){% endfor %}.{% endif %}

## People

{% for ass in associated_with if ass.object.schema_type == "xyzri:XYZPerson" and not ass.ended -%}
- {{ ass.object.given_name }}&nbsp;{{ ass.object.family_name }} ({{ ass.roles|join(" and ", attribute="display_label") }})
{% endfor -%}
